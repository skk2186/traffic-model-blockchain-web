你正在协助我改造当前 VS Code 工作区中的 WeCross-WebApp 前端项目。

## 一、项目背景

当前项目基于 WeBankBlockchain/WeCross-WebApp，技术栈主要为：

- Vue 2
- Vue Router 3
- Vuex 3
- Element UI 2
- Axios
- ECharts 4
- SCSS
- Vue CLI 4

现有项目是 WeCross 跨链管理平台，已经具备：

- 登录、注册和 Token 管理
- admin/user 角色权限
- 动态路由
- 链上账户管理
- Router 管理
- 跨链资源管理
- 普通交易管理
- XA 跨链事务管理
- 系统状态和区块链状态展示
- 后端接口封装和统一错误处理

## 二、改造后的业务定位

产品名称暂定为：

“交通大模型数据可信共享与隐私保护平台”

英文副标题可使用：

“Blockchain-Enabled Cloud-Edge-End Federated Collaboration Platform”

业务背景：

构建一个以“区块链 + 云边端联邦协同”为核心的交通大模型数据共享与隐私保护技术方案。

平台需要体现以下业务概念：

- 交通大模型
- 交通数据资产
- 云边端协同
- 区块链可信存证
- 跨域数据共享
- 链上身份认证
- 数据访问控制
- 共享行为审计
- 跨链事务
- 联邦协同

## 三、总体改造原则

本次改造的核心要求是：

保留现有后端业务、接口协议和主要业务逻辑，重构前端界面、视觉样式、菜单名称、信息架构和业务表达。

采用“保留业务内核，重构表现层”的方案，不从零重写项目，不升级到 Vue 3，不替换 Element UI，不随意升级主要依赖。

## 四、必须保留的内容

除非我明确要求，否则不要修改以下内容：

1. src/api 中的接口 URL。
2. 后端请求参数字段名。
3. 后端响应字段名。
4. Authorization 请求头格式。
5. Token 的保存和读取方式。
6. admin、user 等后端角色值。
7. 资源路径格式。
8. 普通交易的请求结构。
9. XA 跨链事务的请求结构。
10. 国密、签名、哈希、账户密钥相关逻辑。
11. Axios 拦截器的核心认证和错误处理逻辑。
12. 后端业务接口和后端代码。

显示名称可以改变，但传递给后端的数据结构不能改变。

例如：

- 页面上可以把 resource 显示为“交通数据资产”。
- 代码向后端提交时仍然使用原来的 resource 字段。
- 页面上可以把 transaction 显示为“可信共享记录”。
- API 请求仍然使用原来的交易接口和字段。

## 五、建议保留的目录

以下目录以复用为主：

- src/api
- src/utils
- src/store/modules/user.js
- src/store/modules/permission.js
- src/permission.js

以下目录可以局部修改：

- src/router
- src/store/modules/app.js
- src/layout

以下目录是主要重构区域：

- src/views/homepage
- src/views/login
- src/views/router
- src/views/resource
- src/views/transaction
- src/views/account
- src/views/access
- src/layout/components
- src/styles
- src/assets

## 六、菜单业务化映射

将原来的技术名称调整为交通大模型业务名称，但第一阶段不要修改 path、route name、component 和 roles。

建议映射如下：

- 平台首页 → 交通数据可信协同总览
- 账户管理 → 链上身份管理
- 路由管理 → 云边端协同节点
- 资源管理 → 交通数据资产
- 交易管理 → 可信共享记录
- 事务管理 → 跨域协同任务
- 权限管理 → 参与方与访问控制
- 参考文档 → 平台使用指南

路由 path 和 name 暂时保持原样，避免影响页面跳转、权限和 activeMenu。

## 七、首页改造目标

首页应从传统区块链运维后台，改造成交通大模型可信协同驾驶舱。

首页建议包含：

1. 平台标题和业务说明。
2. 核心指标卡片。
3. 云边端协同拓扑。
4. 区块链网络运行状态。
5. 交通数据资产列表。
6. 数据共享或交易记录。
7. 隐私保护与可信能力说明。

现有数据可以进行如下展示映射：

- chainNumber → 协同链网络
- routerNumber → 云边协同节点
- resourceNumber → 可信数据资产
- blockNumber → 可信账本高度
- transaction → 可信共享记录
- XA transaction → 跨域协同任务
- account → 链上身份
- chain path → 协同网络标识

不能伪造后端不存在的实时数据。

以下内容如果没有真实接口，只能作为静态能力说明，不能显示为真实运行指标：

- 联邦训练准确率
- 联邦学习轮次
- 模型参数量
- 差分隐私预算
- 模型聚合状态
- 真实端设备数量
- GPU 使用率
- 模型推理吞吐量

## 八、视觉风格

整体采用“交通行业 + 数字科技 + 区块链可信协同”的视觉风格。

建议配色：

- 页面主背景：#061426
- 侧边栏背景：#071A32
- 卡片背景：#0D223B
- 主色：#168CFF
- 强调色：#37E2FF
- 成功色：#2ED6A1
- 警告色：#FFB84D
- 主文字：#EAF6FF
- 次文字：#91ACC7

界面要求：

- 深色科技风。
- 保持信息清晰，不要过度使用发光效果。
- 卡片边界清晰。
- 表格仍然易读。
- 状态颜色统一。
- 避免大量内联 style。
- 尽量把公共样式放入 SCSS。
- 优先复用 Element UI 组件。
- 不引入大型新依赖，除非确有必要并先说明原因。

## 九、代码质量要求

每次修改必须做到：

1. 先检查当前代码和关联调用，再修改。
2. 不删除仍被其他页面使用的组件。
3. 不改变后端字段含义。
4. 不随意重命名公共方法。
5. 避免一次修改过多不相关文件。
6. 尽量消除新增的内联样式。
7. 保持 Vue 2 Options API 写法。
8. 保持项目现有 ESLint 风格。
9. 新增业务映射时，用单独方法或配置对象处理，不要污染原始后端数据。
10. API 异常时保留合理的空状态和错误提示。
11. 不通过 Mock 数据掩盖接口错误。
12. 不执行 npm audit fix --force。
13. 不擅自升级 Vue、Vue Router、Vuex、Element UI、Webpack 或 Axios。

## 十、每次工作的固定流程

收到具体修改任务后，请按照以下顺序工作：

1. 查看 git status。
2. 阅读与本次任务相关的代码。
3. 简要说明准备修改哪些文件以及原因。
4. 实施修改。
5. 检查 git diff。
6. 运行适用的验证命令。
7. 汇总修改内容、验证结果和剩余风险。

可使用的验证命令：

- npm run lint
- npm run build:prod

如果验证失败：

- 明确报告错误。
- 判断错误是否由本次修改引起。
- 不要隐藏错误。
- 不要通过大范围升级依赖解决局部问题。

## 十一、输出格式

每次完成任务后，请按以下格式汇报：

### 修改文件
列出文件及用途。

### 主要改动
说明实际完成的内容。

### 保留不变
说明哪些接口、字段和业务逻辑未修改。

### 验证结果
列出执行过的命令及结果。

### 风险或待办
列出未解决的问题。

现在先阅读项目，不要立即进行大范围重写。后续按照我给出的阶段任务逐步修改。