vue.config.js
在proxy中修改IP为对应服务器IP

src\utils\rsa.js
删去JSEncrypt(null)中的null

第一阶段：品牌、主题和菜单改造
修改文件列表
vue.config.js：页面构建标题改为“交通大模型可信协同平台”。
src/utils/get-page-title.js：浏览器标题品牌名改为“交通大模型可信协同平台”。
src/router/index.js：仅修改用户可见的 meta.title 菜单名称。
src/layout/index.vue：更新 intro.js 帮助引导文案。
src/layout/components/Sidebar/Logo.vue：侧边栏品牌名和品牌区深色样式。
src/layout/components/Navbar.vue：顶部导航深色样式，用户菜单“账户管理”改为“链上身份管理”。
src/components/Breadcrumb/index.vue：面包屑默认品牌名和深色文字。
src/styles/variables.scss：侧边栏主题变量改为交通科技深色风，宽度改为 220px。
src/styles/index.scss：页面主背景、通用容器、链接、aside 基础深色样式。
src/styles/element-ui.scss：补充 Element UI 卡片、表格、输入框、下拉框、弹窗、分页、空状态的基础深色覆盖。
src/views/login/index.vue：登录页标题改为新平台名，背景改为 #061426。
src/views/register/index.vue：注册页标题改为新平台名，背景改为 #061426。
src/store/modules/user.js：仅修改首次使用引导的用户可见品牌文案和标题。
主要改动
菜单已按要求业务化，例如“平台首页”改为“交通数据可信协同总览”，“资源管理”改为“交通数据资产”，“交易管理”改为“可信共享记录”，“权限管理”改为“参与方与访问控制”。
全局主题已切到深色交通科技风：侧边栏、顶部导航、页面背景、Element UI 卡片/表格/输入/下拉/弹窗/分页/空状态都有基础样式覆盖。
保留不变
未修改 src/api、接口 URL、请求字段、响应字段、Authorization、Token 保存读取、admin/user 角色值、权限逻辑、国密/签名/哈希/账户密钥逻辑。
src/router/index.js 中未改 path、name、component、redirect、roles、activeMenu，只改了 meta.title。
验证结果
npm run lint：通过。首次沙箱内运行因 Node 访问 C:\Users\10851 权限失败，提升权限重跑后通过。
npm run build:prod：通过。构建有原项目常见体积 warning：issue.png、chunk-elementUI、chunk-libs 超过推荐大小；不是本次改动导致的编译错误。
补充：任务要求检查的 src/settings.js 当前项目中不存在；src/layout/components/Navbar 是 Navbar.vue 单文件，不是目录。
风险或待办
本阶段只做全局基础主题，具体业务页面内部可能仍有局部浅色样式或旧业务表达，后续阶段需要逐页细化。
建议 Git commit message：
feat: 第一阶段：品牌、主题和菜单改造